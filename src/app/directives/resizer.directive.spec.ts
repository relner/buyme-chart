import { ResizerDirective } from './resizer.directive';

describe('ResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizerDirective(null);
    expect(directive).toBeTruthy();
  });
});
